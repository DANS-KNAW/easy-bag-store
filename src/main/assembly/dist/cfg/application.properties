bag-store.base-dir={{ easy_bag_store_basedir }}
bag-store.base-uri={{ easy_bag_store_baseuri }}

# Positions in the UUID where to insert a slash when creating an item-location
bag-store.slash-positions={{ easy_bag_store_slashpositions }}